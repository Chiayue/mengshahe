
-- 这里统一写NPC调用的通用逻辑函数,比如游戏结束等...


-- 游戏结束
function GameOver(trigger)
    -- local herocount = HeroList:GetHeroCount()
    -- for playerid = 0, herocount-1 do
    --     local steam_id =  PlayerResource:GetSteamAccountID(playerid)
    --     game_request:saveOrUpdate(steam_id)
    -- end
    
    -- print("Game Over!");
    -- GameRules:MakeTeamLose(global_var_func:GloFunc_Getgame_enum().TEAM_TH);
   
end